<h1>Sửa khuyến mãi</h1>
<form action="/edit-promotion/<%= data.promotion.makhuyenmai %>" method="POST">
  <label for="promotionName">Tên khuyến mãi:</label>
  <input type="text" name="promotionName" value="<%= data.promotion.tenkhuyenmai %>" required>

  <label for="discountPercentage">Phần trăm giảm giá:</label>
  <input type="number" name="discountPercentage" value="<%= data.promotion.phantramgiamgia %>" required min="1" max="100">

  <label for="startDate">Ngày bắt đầu:</label>
  <input type="datetime-local" name="startDate" value="<%= data.promotion.formattedStartDate %>" required>

  <label for="endDate">Ngày kết thúc:</label>
  <input type="datetime-local" name="endDate" value="<%= data.promotion.formattedEndDate %>" required>

  <button type="submit">Cập nhật khuyến mãi</button>
</form>
